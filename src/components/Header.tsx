import { IonHeader, IonTitle, IonToolbar } from "@ionic/react";

const Header: React.FC = () => {
  return (
    <IonHeader>
      <IonToolbar>
        <IonTitle>AARON GESMER</IonTitle>
      </IonToolbar>
    </IonHeader>
  );
};

export default Header;
